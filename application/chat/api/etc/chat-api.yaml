Name: chat-api
Host: 0.0.0.0
Port: 8087

Auth:
  AccessSecret: ${JWT_ACCESS_SECRET}
  AccessExpire: 604800

ChatRpc:
  Etcd:
    Hosts:
      - ${ETCD_HOST}:${ETCD_PORT}
    Key: chat.rpc

BizRedis:
  Host: ${REDIS_HOST}:${REDIS_PORT}
  Pass: ${REDIS_PASS}
  Type: node
