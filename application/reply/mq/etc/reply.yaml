Name: mq
KqConsumerConf:
  Name: reply-kq-consumer
  Brokers:
    - ${KAFKA_HOST}:${KAFKA_PORT}
  Group: group-thinktalk-reply
  Topic: thinktalk_reply
  Offset: last
  Consumers: 1
  Processors: 1
Mysql:
  DataSource: ${DB_USER}:${DB_PASS}@tcp(${DB_HOST}:${DB_PORT})/thinktalk_reply?charset=utf8mb4&parseTime=true&loc=Asia%2FShanghai
CacheRedis:
    - Host: ${REDIS_HOST}:${REDIS_PORT}
      Type: node
      Pass: ${REDIS_PASS}
